@model WAG.ViewModels.ArtisticWorks.DeleteCategoryViewModel

@{
    ViewData["Title"] = "DeleteCategory";

    Cloudinary cloudinary = Model.Cloudinary;
}

<header class="container header-default">
    <div class="row">
        <div class="col-sm-12 col-12">
            <h1 class="text-center">Delete Category</h1>
        </div>
    </div>
</header>

<main class="container main-default">
    <div class="row">

        <aside class="col-sm-3 col-12 left-panel">
            <img src="@Html.Raw(cloudinary.Api.UrlImgUp.Secure(true).Format("jpg")
                             .Transform(new Transformation().Width(200).Quality(100))
                             .BuildUrl(Model.ArtWorkCategory.MainPictureFileName))" style="padding-bottom:25px" />
            <div>
                <p>Name: <strong>@Model.ArtWorkCategory.Name</strong></p>
            </div>
        </aside>

        <section class="col-sm-9 col-12 right-panel">
            <div>
                <h2>Are you sure you want to delete permanently this Category?</h2>
                <br />
                <div>
                    <form method="post" asp-area="Administration" asp-controller="ArtisticWork" asp-action="DeleteCategoryPost" asp-route-id="@Model.ArtWorkCategory.Id">
                        <button type="submit" class="btn btn-warning">Delete</button>
                    </form>
                </div>
            </div>
        </section>

    </div>
</main>

